# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (7):**
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/api ^1.9.0 (already present)" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/api ^1.9.0 (already present)" unless a runtime/agent needs it without an import._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/sdk-node ^0.200.0 (already present)" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/sdk-node ^0.200.0 (already present)" unless a runtime/agent needs it without an import._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/exporter-trace-otlp-http ^0.200.0 (already present)" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/exporter-trace-otlp-http ^0.200.0 (already present)" unless a runtime/agent needs it without an import._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/exporter-metrics-otlp-http ^0.200.0" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/exporter-metrics-otlp-http ^0.200.0" unless a runtime/agent needs it without an import._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/sdk-metrics ^2.0.0" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/sdk-metrics ^2.0.0" unless a runtime/agent needs it without an import._
- ⚠ qr-metric-naming (`src/config/tracing.js`:31): The new counter name 'http.server.request.outcomes' pluralizes the noun and encodes an outcome dimension in the name; semconv counts are singular/unsuffixed nouns (e.g. http.server.request), and outcome belongs in attributes (which it already is). Consider naming it 'http.server.request.count' or aligning to a semconv-shaped singular noun. — _Rename to a singular semconv-style name such as 'http.server.request' (outcome kept as an attribute)._
- ⚠ qr-preserves-behavior (`src/config/tracing.js`:66): httpRequestOutcomes records 'http.response.status_code': 0 when errorType is set (connection closed), emitting a status code of 0 as a metric attribute which is not a valid HTTP status and pollutes the timeseries. This does not change application behavior but produces incorrect telemetry. — _Omit the http.response.status_code attribute entirely when there is no response status, rather than recording 0._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `src/config/tracing.js`
- `src/index.js`
- `src/controllers/auth.controller.js`
- `package.json`

## Dependencies
- `@opentelemetry/api ^1.9.0 (already present)`
- `@opentelemetry/sdk-node ^0.200.0 (already present)`
- `@opentelemetry/exporter-trace-otlp-http ^0.200.0 (already present)`
- `@opentelemetry/exporter-metrics-otlp-http ^0.200.0`
- `@opentelemetry/sdk-metrics ^2.0.0`
