# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (3):**
- qr-unwired-telemetry (`src/config/tracing.js`): httpMetricsMiddleware records both http.server.request.duration and http.server.request.outcomes but is never attached to the Express app anywhere in the diff (no app.use in src/app.js). The middleware never runs, so neither the latency histogram nor the availability counter ever records.
- sli-not-implemented (`src/config/tracing.js`): node-express-boilerplate-http-availability has no reachable measurement point: its backing counter (http.server.request.outcomes) lives only in httpMetricsMiddleware, which is never mounted on the app, so the availability SLI is not implemented.
- sli-not-implemented (`src/config/tracing.js`): node-express-boilerplate-http-latency-p95 has no reachable measurement point: its backing histogram (http.server.request.duration) is only recorded in httpMetricsMiddleware, which is never mounted, so the P95 latency SLI is not implemented.
**Outstanding (1):**
- ⚠ qr-metric-naming (`src/config/tracing.js`:30): The counter name 'http.server.request.outcomes' is not a semconv metric — semconv uses http.server.request.count-style / the outcome is normally an attribute on a plain unsuffixed counter. A pluralized 'outcomes' noun encodes the attribute dimension into the name. — _Rename to a plain unsuffixed counter such as 'http.server.requests' (or use the standard http.server.request.duration count) with the outcome/status carried as attributes._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `src/config/tracing.js`
- `src/index.js`
- `src/controllers/auth.controller.js`
- `package.json`
- `src/app.js`

## Dependencies
- `@opentelemetry/api@^1.9.0 (already present)`
- `@opentelemetry/exporter-metrics-otlp-http@^0.200.0`
- `@opentelemetry/sdk-metrics@^2.0.0`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
