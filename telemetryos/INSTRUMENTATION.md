# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (5):**
- qr-unwired-telemetry (`src/config/tracing.js`): httpMetricsMiddleware is exported but never attached to the Express app via app.use — it never runs. Consequently httpServerRequestDuration and httpServerRequestOutcomes are never recorded, leaving both the HTTP availability and P95 latency SLIs unmeasured.
- sli-not-implemented (`src/config/tracing.js`): The http-availability SLI is backed by httpServerRequestOutcomes/httpServerRequestDuration, both recorded only inside httpMetricsMiddleware which is never wired into the app, so no measurement point for node-express-boilerplate-http-availability actually executes.
- sli-not-implemented (`src/config/tracing.js`): The http-latency-p95 SLI relies on httpServerRequestDuration recorded in httpMetricsMiddleware, which is never attached to the app; nothing in the diff records request latency.
- qr-metric-naming (`src/config/tracing.js`): The new counter name 'http.server.request.outcomes' uses 'outcomes' as a namespace-level noun, whereas the outcome is already carried as an attribute; a plain request count should be unsuffixed (e.g. http.server.request.count is discouraged too — prefer http.server.requests or reuse the semconv duration count).
- qr-not-a-noop (`src/config/tracing.js`): The try/catch around sdk.start() swallows real startup failures and, with the comment claiming an agent may already be registered, allows the process to continue after the SDK failed to register. Because metrics.getMeter() is resolved right after and OTel-JS has no metrics proxy, the histogram/counters would bind to a NoopMeter that never rebinds if start() actually threw.
**Outstanding (1):**
- ⚠ qr-metric-naming (`src/config/tracing.js`:38): The counter name 'http.server.requests' collides semantically with the semconv histogram 'http.server.request.duration' but drops the singular 'request' segment used elsewhere; the repo mixes 'http.server.request.*' and 'http.server.requests' for closely related concepts. — _Rename the counter to a consistent name such as 'http.server.request.count' to match the 'http.server.request.*' namespace already established._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `src/config/tracing.js`
- `src/index.js`
- `src/controllers/auth.controller.js`
- `package.json`
- `src/app.js`

## Dependencies
- `@opentelemetry/api@^1.9.0`
- `@opentelemetry/exporter-metrics-otlp-http@^0.200.0`
- `@opentelemetry/sdk-metrics@^2.0.0`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
