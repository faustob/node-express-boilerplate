# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `179ae84`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`src/index.js`): index.js requires('../instrumentation') which calls @vercel/otel registerOTel, but that require happens AFTER `express` and the auto-instrumentation targets have effectively been loaded via the module graph (app, mongoose imported right after) — more critically, the actual SDK bootstrap in otel.js (NodeSDK.start with auto-instrumentations, exporters, metric reader) is never required or preloaded anywhere, so it never runs. registerOTel from @vercel/otel does not by itself configure OTLP exporters to any collector.
- general (`instrumentation.js`): Two competing SDK bootstraps exist: instrumentation.js (registerOTel) and otel.js (NodeSDK.start with auto-instrumentations). If both were ever loaded they would register conflicting global providers/instrumentations; only one should be the registration path.
- general (`package.json`): Dependency pins mix OTel SDK 1.x-line packages (@opentelemetry/resources, sdk-metrics @^1.28.0) with SDK 0.55.x exporters/sdk-node; otel.js uses resourceFromAttributes and ATTR_SERVICE_NAME which require the 2.x resources/semconv API, contradicting the ^1.28.0 pins per the stated 2.0 target.
- qr-not-a-noop (`otel.js`): NodeSDK.start() does not throw synchronously; wrapping it in a try/catch to 'tolerate an already-registered provider' does not catch async registration errors, so the guard is ineffective for its stated purpose.
- dependency-unused (`package.json`): Declared dependency "@opentelemetry/sdk-trace-node" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses).
- qr-not-a-noop (`src/index.js`): src/index.js requires('../otel') which starts a hand-built NodeSDK, but the repo also contains instrumentation.js which calls @vercel/otel registerOTel. If both are loaded they register competing global providers; regardless, index.js loads only otel.js, so the two registration files conflict and one path is dead. This is a duplicate/conflicting SDK registration.
- general (`otel.js`): The wiring recipe and otelTarget prescribe @vercel/otel registerOTel for this Node service, but otel.js hand-builds a NodeSDK with OTLP exporters; telemetryCollection is 'none', so there is no collector endpoint and the OTLP exporters will fail to export at runtime.
**Outstanding (1):**
- ⛔ qr-not-a-noop (`instrumentation.js`:1): This is a plain Express app, not Next.js — nothing invokes Next's `register()` convention. `@vercel/otel`'s `registerOTel` is designed for the Next.js runtime; calling it in a preloaded module for a plain Node/Express server does not wire up a working SDK with exporters/tracer provider, so the getMeter/getTracer instruments in auth.controller.js resolve to no-op providers and emit no telemetry. — _For a plain Node/Express app, register a real SDK at startup using @opentelemetry/sdk-node (NodeSDK with a metric/trace exporter and auto-instrumentations), preloaded via `node -r ./otel.js`, instead of @vercel/otel's registerOTel._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour
- **Node-express-boilerplate — HTTP Response Time P99** (`node-express-boilerplate-http-latency-p99`) — target < 750ms over 1-hour
- **Node-express-boilerplate — HTTP 5xx Error Rate** (`node-express-boilerplate-http-error-rate`) — target < 0.1% over 1-hour
- **Node-express-boilerplate — HTTP Worker Pool Saturation** (`node-express-boilerplate-http-saturation`) — target < 80% over 5-minute
- **Node-express-boilerplate — Authentication Failure Rate** (`node-express-boilerplate-http-auth-failure-rate`) — target < 2% over 1-hour
- **Node-express-boilerplate — HTTP Request Throughput** (`node-express-boilerplate-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `src/index.js`
- `src/controllers/auth.controller.js`
- `package.json`

## New files
- `instrumentation.js`

## Dependencies
- `@vercel/otel`
- `@opentelemetry/api`
