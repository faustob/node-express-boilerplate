# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (3):**
- qr-unwired-telemetry (`src/index.js`): httpMetricsMiddleware is attached via app.use() AFTER the routes are already registered inside the imported './app' module. Express runs middleware in registration order, so this middleware sits behind all route handlers and its res.on('finish') never fires for matched routes — the HTTP duration histogram and outcome counter never record for real requests.
- qr-metric-naming (`src/config/tracing.js`): The new counter name 'http.server.request.outcomes' uses a plural noun for a plain count where the semconv/corpus style for counts is a singular unsuffixed noun (e.g. http.server.request.count-style is unsuffixed but not pluralized); this is inconsistent with the singular auth.registrations/auth.logins concept style is fine but the plural on a namespaced http.server metric is a naming mismatch.
- qr-preserves-behavior (`src/index.js`): src/index.js still imports httpMetricsMiddleware (last hunk removed the app.use call but the import destructure remains as dead code); combined with app.js it is a leftover unused binding.
**Outstanding (1):**
- ⛔ qr-unwired-telemetry (`src/index.js`:9): httpMetricsMiddleware is attached via app.use in src/index.js AFTER app = require('./app') has already registered all routes and error handlers. In Express, middleware registered after the routes never runs for those routes, so the HTTP duration histogram and outcome counter backing the availability and P95 SLIs never record. — _Attach httpMetricsMiddleware inside src/app.js before the route registrations (e.g. right after app initialization), not on the returned app in index.js after routes are mounted._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `package.json`
- `src/config/tracing.js`
- `src/index.js`
- `src/controllers/auth.controller.js`

## Dependencies
- `@opentelemetry/api@^1.9.0`
- `@opentelemetry/sdk-node@^0.200.0`
- `@opentelemetry/exporter-trace-otlp-http@^0.200.0`
- `@opentelemetry/exporter-metrics-otlp-http@^0.200.0`
- `@opentelemetry/sdk-metrics@^2.0.0`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
