# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (3):**
- qr-not-a-noop-static (`src/config/tracing.js`): Stubbed instrumentation: a meter/instrument is created but the file only console.warn/logs and never records (no .record/.add/.observe).
- qr-not-a-noop (`src/config/tracing.js`): The meter, counter, and histogram are created at module-load time in the same module that calls sdk.start(). Because NodeSDK.start() is synchronous and runs before metrics.getMeter() executes below it, the instruments actually bind after registration here — but sdk.start() is wrapped in a try/catch that swallows startup failures, so if start() throws (e.g. exporter/config error), the process continues with the metrics API never registered and metrics.getMeter() returns a NoopMeter whose instruments never rebind, silently dropping both SLIs.
- qr-metric-naming (`src/config/tracing.js`): The new counter name 'http.server.request.outcomes' encodes the outcome as part of the name while the outcome is also carried as an attribute, and 'outcomes' is a non-semconv plural noun; the standard semconv counter for this is http.server.request.count/http.server.request with outcome as a dimension.
**Outstanding (1):**
- ⚠ qr-metric-naming (`src/config/tracing.js`): The new counter 'http.server.request' collides conceptually with semconv's http.server.request.duration histogram and is unsuffixed; a request count matching the SLI would be clearer as 'http.server.request.count' or use the existing semconv-derived count. The name 'http.server.request' as a plain counter is ambiguous against the histogram of the same base namespace. — _Rename the counter to something like 'http.server.request.count' or reuse the auto-instrumentation's http.server.request.duration count, keeping a distinct base from the histogram._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `src/config/tracing.js`
- `src/index.js`
- `src/app.js`
- `package.json`

## Dependencies
- `@opentelemetry/exporter-metrics-otlp-http@^0.200.0`
- `@opentelemetry/sdk-metrics@^2.0.0`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
