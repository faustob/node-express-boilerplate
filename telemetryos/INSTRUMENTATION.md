# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `7a13663`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-metric-naming (`src/config/tracing.js`): The new counter is named 'http.server.request.count', but semconv-style plain counts are unsuffixed — the '.count' suffix encodes the implementation. It should be a plain unsuffixed name such as 'http.server.request' or use the standard 'http.server.request.duration' histogram's count for volume.
- qr-metric-naming (`src/config/tracing.js`): The new counter is named 'http.server.request.outcomes', which is a plural, non-standard name for an outcome-classified request count; semconv/count convention favors an unsuffixed singular count (e.g. 'http.server.request.count' or reuse of the request duration histogram's count).
**Outstanding (1):**
- ⚠ general (`src/config/tracing.js`:21): The auto-instrumentations-node package (and its instrumentation-http) is used but not among the proposedDependencies verified as present, and the http auto-instrumentation already emits http.server.request.duration, which duplicates the manually-created histogram of the same name. — _Either rely on the auto-instrumentation's http.server.request.duration and drop the manual histogram, or give the manual instrument a distinct name; and ensure auto-instrumentations-node is declared (it is in package.json)._

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `src/config/tracing.js`
- `src/app.js`
- `src/index.js`
- `package.json`

## Dependencies
- `@opentelemetry/exporter-metrics-otlp-http@^0.200.0`
- `@opentelemetry/sdk-metrics@^2.0.0`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
