# Instrumentation for Node-express-boilerplate

Generated by TelemetryOS against `997285d`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · yarn · host: docker · telemetry: otel-collector → wiring model **app-sdk**
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- dependency-auto-added (`package.json`): Added @opentelemetry/auto-instrumentations-node@^0.57.0 to package.json — imported by the instrumentation but not declared.
- qr-not-a-noop (`src/middlewares/telemetry.js`): The telemetry middleware is defined and exported but nothing in the diff wires it into the Express app (no app.use in src/app.js), so the custom counter and histogram never record — the instrumentation emits nothing.
- qr-not-a-noop (`otel.js`): The OTLPTraceExporter and OTLPMetricExporter are constructed with `undefined` when OTEL_EXPORTER_OTLP_ENDPOINT is unset, causing them to default to localhost — combined with the collector-based deployment this means traces/metrics silently go nowhere if the env var is missing.
- general (`src/middlewares/telemetry.js`): The new histogram name 'http.server.request.duration.business' deviates from the OTel semantic-convention name http.server.request.duration and records milliseconds while semconv durations use seconds; a bespoke suffix and unit is introduced instead of a convention-shaped name.
- qr-not-a-noop (`otel.js`): The SIGTERM handler in otel.js calls sdk.shutdown() but never re-raises or forwards to the app's own SIGTERM handler; however the real defect is that src/index.js's SIGTERM handler closes the server without flushing/awaiting the SDK — the metric/trace exporters may lose buffered data on shutdown. This is a telemetry-loss issue but not a hard no-op; omitting.
- general (`package.json`): package.json contains duplicate keys for "@opentelemetry/instrumentation-http" and "@opentelemetry/instrumentation-express" in the dependencies object (each appears twice). Duplicate JSON keys are invalid/ambiguous and the second hunk re-adds names already inserted by the first hunk.
**Outstanding (4):**
- ⛔ qr-not-a-noop-static (`src/middlewares/telemetry.js`:5): Commented-out instrumentation: instrument creation or recording is commented out, so this file emits no telemetry. — _Emit working telemetry or add nothing — never leave commented-out instrument/record calls._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/instrumentation-http" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/instrumentation-http" unless a runtime/agent needs it without an import._
- ⚠ dependency-unused (`package.json`): Declared dependency "@opentelemetry/instrumentation-express" is not imported by any instrumentation file in this change — likely unnecessary (the wiring recipe should list only what the code uses). — _Remove "@opentelemetry/instrumentation-express" unless a runtime/agent needs it without an import._
- ⛔ qr-not-a-noop (`otel.js`:40): The SIGTERM handler in otel.js calls sdk.shutdown() but does not call process.exit or coordinate with src/index.js's own SIGTERM handler; however the primary defect is that on shutdown both handlers race — this is acceptable. The real issue: metrics use PeriodicExportingMetricReader which flushes only periodically, and on SIGTERM sdk.shutdown() runs concurrently with server.close(), but pending metrics may be exported. This does not break emission. — _omit_

## SLIs implemented
- **Node-express-boilerplate — HTTP Service Availability** (`node-express-boilerplate-http-availability`) — target 99.95% over 30-day
- **Node-express-boilerplate — HTTP Response Time P95** (`node-express-boilerplate-http-latency-p95`) — target < 200ms over 1-hour

## Modified files
- `package.json`
- `src/index.js`
- `src/app.js`

## New files
- `otel.js`
- `src/middlewares/telemetry.js`

## Dependencies
- `@opentelemetry/api`
- `@opentelemetry/sdk-node`
- `@opentelemetry/exporter-trace-otlp-http`
- `@opentelemetry/exporter-metrics-otlp-http`
- `@opentelemetry/sdk-metrics`
- `@opentelemetry/auto-instrumentations-node`
- `@opentelemetry/instrumentation-http`
- `@opentelemetry/instrumentation-express`
- `@opentelemetry/resources`
- `@opentelemetry/semantic-conventions`
- `@opentelemetry/auto-instrumentations-node@^0.57.0`
