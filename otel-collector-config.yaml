# Generated by TelemetryOS — managed collector pipeline.
# Do not edit by hand: this file is a render of the pipeline spec;
# changes are made in TelemetryOS (Backend tab) and regenerated via PR.
# Spec version: 1

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:

exporters:
  otlphttp:
    endpoint: ${env:TELEMETRYOS_OTLP_ENDPOINT}
    headers:
      authorization: ${env:TELEMETRYOS_OTLP_HEADERS}

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp]
